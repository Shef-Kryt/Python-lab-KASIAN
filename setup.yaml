pipeline {
    agent any

    stages {
        stage('Setup Server') {
            steps {
                script {
                    // Оновлення системи
                    echo 'Оновлення системи'

                    // Встановлення nginx
                    echo 'Встановлення nginx'

                    // Встановлення VPN сервера
                    // Додайте сюди команди для встановлення VPN сервера за допомогою apt

                    // Перезапуск сервісу nginx
                    echo 'systemctl restart nginx'
                }
            }
        }
    }
}
